# Live evidence — matmul

**Date:** generated by run_live_optimize
**Op / shapes:** `matmul` {"A": [512, 512], "B": [512, 512]}
**Model:** yunwu/claude-sonnet-4-6
**Target HW:** nvidia_ampere (RTX 3060 Laptop, SM 8.6, 6 GB)

## Result

| Metric | Value |
|:--|:--|
| Stop reason | max_turns |
| Decisions | 10 |
| Decisions with @rationale | 10/10 |
| Tool calls | 25 |
| compile_and_profile calls (correct) | 2 |
| Tokens (in/out) | 140789 / 3438 |
| Duration | 124.17s |
| **Best latency (real GPU)** | 0.0797 ms |
| **Best baseline_ratio** | 0.4035 |
| Backend | triton |
| Real GPU profile? | ✅ yes — real Triton latency measured |
| Errors | none |

## @rationale trajectory audit (A5)

✅ clean — every decision carries a non-empty rationale


## Real GPU profiles (compile_and_profile results, by latency)

- latency=0.0797 ms, baseline_ratio=0.4035, backend=triton
- latency=0.1746 ms, baseline_ratio=1.6286, backend=triton

## S2 resume provenance

```json
{
  "resumed_from": null,
  "replayed_decisions": 0,
  "replayed_compiles": 0,
  "state_out": "benchmarks/results/phase1/harness_v2/live/matmul/state.json"
}
```

## Decisions (with @rationale)

- `tile({"loop": "i", "factors": [128]})` :: Tile the M-dimension (i) with factor 128. This creates 512/128=4 outer tiles along M. Combined with J tiling this gives 
- `tile({"factors": [128], "loop": "j"})` :: Tile the N-dimension (j) with factor 128. Creates 4 outer tiles along N, giving a 4×4=16 output-block grid across 30 SMs
- `tile({"factors": [32], "loop": "k"})` :: Tile the K reduction axis with factor 32. This sets the shared-memory staging depth: each iteration loads a 128×32 slice
- `parallel({"mapping": {"i": "threadblock.x"}})` :: Map the outer i-tile loop (4 tiles of 128) to threadblock.x. This distributes the M-dimension work across SM grid column
- `parallel({"mapping": {"j": "threadblock.y"}})` :: Map the outer j-tile loop (4 tiles of 128) to threadblock.y. Combined with i→threadblock.x, this creates a 4×4=16 thread
- `vectorize({"loop": "j", "width": 4})` :: Vectorize the inner j loop with width=4. On Ampere this emits 128-bit (float4) memory loads/stores, maximising memory-bu
- `unroll({"loop": "k", "factor": 4})` :: Unroll the inner k loop by factor 4. With k tile=32, this unrolls 4 iterations of the inner-most accumulation loop, redu
- `parallel({"mapping": {"i": "warp"}})` :: Map the inner i-loop (128 elements) to warp-level parallelism. This subdivides the i-tile among warps within each thread
- `unroll({"factor": 4, "loop": "i"})` :: Unroll the inner i-loop by factor 4 to expose ILP along the M-dimension within each warp's register tile. Each unrolled 
- `unroll({"loop": "j", "factor": 4})` :: Unroll the inner j-loop by factor 4 to complement the j-vectorization (width=4 × unroll=4 = 16 j-elements per thread ite

## Provider fallback events (S3)

```json
[]
```
