# Live evidence — flash_attention

**Date:** generated by run_live_optimize
**Op / shapes:** `flash_attention` {"Q": [1, 8, 256, 64], "K": [1, 8, 256, 64], "V": [1, 8, 256, 64]}
**Model:** yunwu/claude-sonnet-4-6
**Target HW:** nvidia_ampere (RTX 3060 Laptop, SM 8.6, 6 GB)

## Result

| Metric | Value |
|:--|:--|
| Stop reason | max_turns |
| Decisions | 8 |
| Decisions with @rationale | 8/8 |
| Tool calls | 23 |
| compile_and_profile calls (correct) | 0 |
| Tokens (in/out) | 117024 / 3059 |
| Duration | 102.01s |
| **Best latency (real GPU)** | n/a |
| **Best baseline_ratio** | n/a |
| Backend | ? |
| Real GPU profile? | ⚠️ none |
| Errors | none |

## @rationale trajectory audit (A5)

✅ clean — every decision carries a non-empty rationale


## Real GPU profiles (compile_and_profile results, by latency)

(none)

## S2 resume provenance

```json
{
  "resumed_from": null,
  "replayed_decisions": 0,
  "replayed_compiles": 0,
  "state_out": "benchmarks/results/phase1/harness_v2/live/flash_attention/state.json"
}
```

## Decisions (with @rationale)

- `tile({"loop": "i", "factors": [64]})` :: Tile the query-sequence dimension i with block size 64. For S=256, D=64, this creates 4 query tiles. A tile of 64 rows ×
- `tile({"factors": [64], "loop": "j"})` :: Tile the key/value-sequence dimension j with block size 64 (Bc=64). This is the inner loop over KV chunks in flash-atten
- `parallel({"mapping": {"b": "threadblock.y"}})` :: Map batch dimension b to threadblock.y. With B=1 this doesn't add parallelism yet but correctly sets up the 2D threadblo
- `parallel({"mapping": {"h": "threadblock.x"}})` :: Map the head dimension h to threadblock.x. With H=8 this creates 8 independent threadblocks across the 30 SMs, each proc
- `place({"tensor": "Q", "memory": "register"})` :: Stage the Q tile in registers. For the 64-row Q tile, keeping the D=64 head dimension row-slice in registers avoids repe
- `place({"memory": "register", "tensor": "K"})` :: Stage K tile in registers. The K block (64×64 fp16) will be loaded once per j-tile and held in registers/SMEM during the
- `place({"memory": "register", "tensor": "V"})` :: Stage V tile in registers. V (64×64 fp16) is loaded per j-tile and used in the final weighted sum O += softmax_weights @
- `parallel({"mapping": {"i": "warp"}})` :: Map the tiled i (query-row) dimension to warps within each threadblock. With i-tile=64 rows and 32 threads/warp, each wa

## Provider fallback events (S3)

```json
[]
```
