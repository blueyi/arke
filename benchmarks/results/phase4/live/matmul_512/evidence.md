# Live evidence — matmul

**Date:** generated by run_live_optimize
**Op / shapes:** `matmul` {"A": [512, 512], "B": [512, 512]}
**Model:** yunwu/claude-sonnet-4-6
**Target HW:** nvidia_ampere (RTX 3060 Laptop, SM 8.6, 6 GB)

## Result

| Metric | Value |
|:--|:--|
| Stop reason | max_turns |
| Decisions | 6 |
| Decisions with @rationale | 6/6 |
| Tool calls | 18 |
| compile_and_profile calls (correct) | 2 |
| Tokens (in/out) | 77580 / 2205 |
| Duration | 75.54s |
| **Best latency (real GPU)** | 0.0661 ms |
| **Best baseline_ratio** | 0.3603 |
| Backend | triton |
| Real GPU profile? | ✅ yes — real Triton latency measured |
| Errors | none |

## @rationale trajectory audit (A5)

✅ clean — every decision carries a non-empty rationale


## Real GPU profiles (compile_and_profile results, by latency)

- latency=0.0661 ms, baseline_ratio=0.3603, backend=triton
- latency=0.1420 ms, baseline_ratio=1.4871, backend=triton

## S2 resume provenance

```json
{
  "resumed_from": null,
  "replayed_decisions": 0,
  "replayed_compiles": 0,
  "state_out": "benchmarks/results/phase4/live/matmul_512/state.json"
}
```

## Decisions (with @rationale)

- `tile({"loop": "i", "factors": [128]})` :: Tile the M-dimension with factor 128 to create 512/128=4 tiles. This matches a typical GEMM block size that fits comfort
- `tile({"loop": "j", "factors": [128]})` :: Tile the N-dimension with factor 128 to create a 128×128 output tile per thread block. Combined with the i-tile of 128, 
- `tile({"factors": [32], "loop": "k"})` :: Tile the K reduction axis with factor 32 to enable software pipelining of shared memory loads. A k-tile of 32 with 128×1
- `parallel({"mapping": {"i": "threadblock.y"}})` :: Map the i (M) outer-tile loop to threadblock.y so each thread block handles a different row-tile. With 512/128=4 tiles i
- `parallel({"mapping": {"j": "threadblock.x"}})` :: Map the j (N) outer-tile loop to threadblock.x so columns are spread across the x-dimension of the grid. Combined with i
- `vectorize({"loop": "j", "width": 4})` :: Vectorize the j (N) inner loop with width 4 to issue 128-bit (4×fp32) or 256-bit wide memory loads/stores. This saturate

## Provider fallback events (S3)

```json
[]
```
