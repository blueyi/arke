# Live evidence — matmul

**Date:** generated by run_live_optimize
**Op / shapes:** `matmul` {"A": [256, 256], "B": [256, 256]}
**Model:** yunwu/claude-sonnet-4-6
**Target HW:** nvidia_ampere (RTX 3060 Laptop, SM 8.6, 6 GB)

## Result

| Metric | Value |
|:--|:--|
| Stop reason | max_turns |
| Decisions | 5 |
| Decisions with @rationale | 5/5 |
| Tool calls | 14 |
| compile_and_profile calls (correct) | 2 |
| Tokens (in/out) | 51337 / 1953 |
| Duration | 69.73s |
| **Best latency (real GPU)** | 0.0695 ms |
| **Best baseline_ratio** | 0.2208 |
| Backend | triton |
| Real GPU profile? | ✅ yes — real Triton latency measured |
| Errors | none |

## @rationale trajectory audit (A5)

✅ clean — every decision carries a non-empty rationale


## Real GPU profiles (compile_and_profile results, by latency)

- latency=0.0695 ms, baseline_ratio=0.2208, backend=triton
- latency=0.1618 ms, baseline_ratio=1.012, backend=triton

## S2 resume provenance

```json
{
  "resumed_from": null,
  "replayed_decisions": 0,
  "replayed_compiles": 0,
  "state_out": "benchmarks/results/phase4/live/matmul_256/state.json"
}
```

## Decisions (with @rationale)

- `tile({"loop": "i", "factors": [64]})` :: Tile the M-dimension (i) with factor 64. A 64-row tile per thread-block is a standard choice for Ampere GEMM — it keeps 
- `tile({"factors": [64], "loop": "j"})` :: Tile the N-dimension (j) with factor 64 to match the i-tile. A symmetric 64×64 output tile per CTA is the textbook block
- `tile({"factors": [16], "loop": "k"})` :: Tile the K-reduction axis with factor 16. A k-tile of 16 means each iteration loads a 64×16 slice of A and a 16×64 slice
- `tile({"factors": [32], "loop": "i"})` :: Switch to a 32-row tile on i. For a 256×256 problem, 64×64 tiles yielded only 16 CTAs (under-utilizing 30 SMs). With til
- `tile({"factors": [32], "loop": "j"})` :: Symmetric 32-column tile on j to pair with the 32-row i-tile. The 32×32 output tile fits cleanly within warp-level MMA f

## Provider fallback events (S3)

```json
[]
```
