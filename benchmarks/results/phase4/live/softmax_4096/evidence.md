# Live evidence — softmax

**Date:** generated by run_live_optimize
**Op / shapes:** `softmax` {"X": [64, 4096]}
**Model:** yunwu/claude-sonnet-4-6
**Target HW:** nvidia_ampere (RTX 3060 Laptop, SM 8.6, 6 GB)

## Result

| Metric | Value |
|:--|:--|
| Stop reason | max_turns |
| Decisions | 3 |
| Decisions with @rationale | 3/3 |
| Tool calls | 13 |
| compile_and_profile calls (correct) | 2 |
| Tokens (in/out) | 47490 / 1631 |
| Duration | 57.88s |
| **Best latency (real GPU)** | 0.0452 ms |
| **Best baseline_ratio** | 0.6140 |
| Backend | triton |
| Real GPU profile? | ✅ yes — real Triton latency measured |
| Errors | none |

## @rationale trajectory audit (A5)

✅ clean — every decision carries a non-empty rationale


## Real GPU profiles (compile_and_profile results, by latency)

- latency=0.0452 ms, baseline_ratio=0.614, backend=triton
- latency=0.0912 ms, baseline_ratio=0.3155, backend=triton

## S2 resume provenance

```json
{
  "resumed_from": null,
  "replayed_decisions": 0,
  "replayed_compiles": 0,
  "state_out": "benchmarks/results/phase4/live/softmax_4096/state.json"
}
```

## Decisions (with @rationale)

- `parallel({"loop": "i"})` :: Each of the 64 rows is independent; mapping row-loop `i` to parallel GPU thread-blocks lets all 30 SMs contribute and el
- `tile({"loop": "j", "factors": [1024]})` :: Tiling the reduction axis `j` with 1024 threads per tile matches the max_threads_per_block=1024 on Ampere. For a 4096-wi
- `vectorize({"loop": "j", "width": 4})` :: Vectorizing the inner reduction loop `j` with width=4 enables 128-bit memory loads (float4), which is the optimal load w

## Provider fallback events (S3)

```json
[]
```
