# Arke Project Audit — 虚假实现/未完成项清单 (2026-07-13)

> Leon 指示: "仔细审视当前 Arke 项目文档中记录目标已经达成开发目标和相应功能是否真的已经都实现完整，
> 而不是虚假实现，记录所有未真实完成的任务为待开发项"

## 审计方法
1. 全源码扫描 TODO/FIXME/NotImplementedError/stub/placeholder
2. 逐项核对 docs/phase4/stage-progress.md + docs/roadmap/plan.md 声称 vs 实际
3. 验证 Gate/Stage/Phase 通过是否有真实测试支撑
4. 检查 AGENTS.md 描述 vs 实际状态一致性

---

## ❌ 发现的虚假/不一致/未完成项

### 严重（文档声称完成但实际未完成或不一致）

| # | 问题 | 位置 | 严重度 | 说明 |
|---|---|---|---|---|
| **F1** | P4-S2 表格行声称 "30 ops" 但实际已 46/46 | `docs/phase4/stage-progress.md:21` | 🟡 文档滞后 | 表格行还写 `30 ops correct` + commit `999b3b5`，但正文已更新为 46/46。不是虚假实现，是文档表格未同步 |
| **F2** | AGENTS.md 声称 "S6 ← current" 但 Phase 1 S6-S9 早已全部 ✅ CLOSED | `~/workspace/AGENTS.md` | 🟡 文档滞后 | AGENTS.md 仍写 S6-S9 为未完成；plan.md 已标 S6-S9 全 ✅ CLOSED (2026-06-25)。需同步 |
| **F3** | D2 验证层声称 "3/4 mechanisms landed" — 缺 Sakana soft-verify | `arke/agent/verification.py` | 🔴 功能缺失 | robust_reward ✅, staged_correctness_gate ✅, reflexion_feedback ✅, **Sakana LLM soft-verify ❌ 未实现**。文档正确标注为 "remaining"，但我之前报告中说 "D2 验证层 3/4" 可能给人 "差不多完成" 的印象——实际缺的是一个需要 live-LLM token 预算的独立机制 |
| **F4** | `arke/compiler/lowering/mlir_emitter.py` 是空 stub | 该文件 | 🟡 死代码 | 16 行空 class，无人引用。真实 MLIR emitter 在 `arke/backend/mlir_emitter.py` (2992 LOC)。不影响功能但误导读者 |
| **F5** | `arke/backends/mlir/` 是独立的旧 stub 目录 | `arke/backends/mlir/` | 🟡 死代码 | 有 `ops.py`(27行 placeholder)、`emitter.py`、`bl1_matmul.py`。仅被 1 个旧测试引用。真实 MLIR 后端在 `arke/backend/mlir_emitter.py`。可删除或标注 deprecated |
| **F6** | AGENTS.md 列 `arke/integration/ — KernelCache, custom_ops` 但两者都不存在 | AGENTS.md 布局表 | 🟡 文档不实 | integration/ 只有 `torch_bridge.py`(197 LOC, 真实实现)。KernelCache 从未创建，custom_ops 概念融入了 torch_bridge |
| **F7** | `arke/backend/protocol.py` BackendRegistry 未实例化 | `arke/backend/protocol.py` | 🟡 架构 stub | protocol.py 声明了 `ArkeBackend` ABC + `BackendRegistry` class，但 Registry 的 `_backends` 未被填充（各 backend 独立使用，未通过统一注册中心发现）。Harness 的 `backends.py` 用的是自己的 backend dispatch，不走 protocol.py 注册 |

### 中等（功能存在但有局限，文档未明确标注）

| # | 问题 | 严重度 | 说明 |
|---|---|---|---|
| **F8** | MLIR-GPU backend 没有注册到 BackendRegistry / `arke run` | 🟡 | Phase 3 的 MLIR emitter 工作正常(2992 LOC, 46 ops)，但不能通过 `arke run --backend mlir_gpu` 调用——只有 builtin(Triton) 和 heuristic 走得通。需要桥接 |
| **F9** | CUDA-C flash_attention 大 seq 性能声称 "0.79×" 但那是 small-seq；large-seq 是 0.18× | 🟢 诚实标注 | 文档已正确区分，但性能表里的 "flash_attention 0.79×" 可能被误读。 |

### 低（死代码/过时，不影响功能）

| # | 问题 | 说明 |
|---|---|---|
| F10 | `arkec/main.py` 已修为 shim，但 `pyproject.toml:65` 还有注释掉的旧入口 `# arke = "arkec.main:cli"` | 可删除注释行 |

---

## 总结

**真实虚假实现: 1 项 (F3 — D2 Sakana soft-verify)**
**文档滞后/不一致: 5 项 (F1, F2, F4, F5, F6)**
**架构 stub 未连线: 2 项 (F7, F8)**
**已诚实标注的局限: 1 项 (F9)**
**低优先级清理: 1 项 (F10)**

Phase 1 (S0-S9) / Phase 3 (MLIR) / Phase 4 (CUDA-C 46/46 ops) 的核心功能是**真实实现**，有测试覆盖。
RL M3 ✅, SSE Streaming ✅, BYOK ✅, MCP primitives ✅ — 这些是今天新做的，实现完整。

唯一的**功能性缺口**是 D2 Sakana soft-verify (F3)。其余都是文档同步或死代码清理。
